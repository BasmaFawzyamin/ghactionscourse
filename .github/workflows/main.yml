name: First Workflow
on:
  push:
    branches:
       - main

jobs: 
   example-job:
     runs-on: ubuntu-latest

     steps:
       - name: print a welcom message
         run: echo " Welcom to our first GitHub Action workflow! "
      
