on:
  workflow_dispatch:
   inputs:
     enviroment:
       description: "choose enviroment"
       required: true
       default: "staging"
       type: string
     

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - name : deploy application
        run : echo " deploy application ${{ inputs.enviroment }}"
